# Portable Fixture Provenance

This fixture contains repository-authored build metadata only. Paths use the
Milestone 0 sentinel roots `/__fixture_src__` and `/__fixture_build__` and do
not identify a host filesystem. It is intentionally independent of a compiler
installation and is used for deterministic serialization tests.

Capture date: 2026-09-04
