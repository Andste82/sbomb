#pragma once
/* Force-included through the precompiled header and used by nothing. It must
 * appear in every dependency file and in no compilation unit's debug info,
 * which is what makes "reached only via the PCH" decidable (section 14.5). */
#define PCH_UNUSED_MARKER 1
